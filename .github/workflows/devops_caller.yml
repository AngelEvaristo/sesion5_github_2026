name: CI/CD dotnet

on:
  workflow_dispatch: {}

permissions:
  id-token: write
  contents: write


jobs:
    ci:
      uses: AngelEvaristo/sesion5_github_2026_template/.github/workflows/dotnet-ci.yml@main
      with:
        dotnetversion: '9.0.x'
        artifactname: 'app-package'
        environment: 'develop'
      secrets: inherit  